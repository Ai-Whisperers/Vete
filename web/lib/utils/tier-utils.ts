export function getTierBenefits(tier: Tier): string[] {
  // Implement logic to get benefits from tier
  return tier.benefits
}

export function getTierStatus(tier: Tier): TierStatus {
  // Implement logic to get status from tier
  return tier.status
}
Note: The above files are just a starting point and may need to be modified to fit the exact requirements of your application. Additionally, you may need to create additional files or modify existing ones to fully implement the tier levels feature.